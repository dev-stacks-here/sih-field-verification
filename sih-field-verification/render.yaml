services:
  # 100% Free Forever Render Static Site (No Docker / No Credit Card Required)
  - type: static
    name: field-verification-platform
    plan: free
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
